spring:
  devtools:
    remote.secret: teste 
    add-properties: true
  jackson:
    serialization:
      indent-output: true
  data:
    mongodb:
      database: employe_test_db
      port: 0
      host:

de:
  flapdoodle:
    mongodb:
      embedded:
        version: 4.0.2 # version compatible with Fedora.

springdoc:
  swagger-ui:
    path: /swagger-ui-custom.html
    operations-sorter: method